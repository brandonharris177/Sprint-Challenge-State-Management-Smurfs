//import {GETING_QUOTE_START, GETING_QUOTE_SUCCESS, GETING_QUOTE_FAILURE} from '../actions'

const initialState = []

const reducer = (state= initialState, action) => {
    switch (action.type) {
        default: return state;
    }
}

export default reducer;